# Database
DB_PASSWORD=guclu_sifre_buraya

# Redis
REDIS_PASSWORD=redis_sifre_buraya

# JWT (openssl rand -hex 64 ile uret)
JWT_SECRET=jwt_secret_buraya
JWT_REFRESH_SECRET=refresh_secret_buraya

# AES-256 Key (openssl rand -hex 32 ile uret - tam 32 karakter olmali)
ENCRYPTION_KEY=encryption_key_buraya
